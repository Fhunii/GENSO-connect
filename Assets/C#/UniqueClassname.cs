using UnityEngine;

[RequireComponent(typeof(ObjectStatusManager))]
[RequireComponent(typeof(SpriteManager))]
[RequireComponent(typeof(DragAndDropManager))]
public class UniqueClassName : MonoBehaviour

{
    void Start()
    {
        // ここで初期化処理などを行う場合は記述します
        
    }

    void Update()
    {
        // 必要に応じて更新処理を行います
        
    }
}
